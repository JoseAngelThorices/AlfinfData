<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AlfinfData.Views.Seleccion.SeleccionPage"
             Title="SeleccionPage"
             BackgroundColor="#e9e9e9">

    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsVisible="False" />
    </Shell.BackButtonBehavior>

    <ScrollView>
        <Grid RowDefinitions="Auto,Auto,Auto,Auto,*" 
              Padding="10" 
              RowSpacing="10">

            <Picker x:Name="GrupoPicker"
                    Grid.Row="1"
                    Title="Selecciona grupo"
                    BackgroundColor="#fbffb8"
                    TitleColor="Black"
                    TextColor="Black"
                    HeightRequest="40"
                    Margin="5" />

            <Grid Grid.Row="2" 
                  ColumnDefinitions="*,*" 
                  ColumnSpacing="10">
                <Button Text="SEL. TODOS"
                        BackgroundColor="#71639e"
                        TextColor="White"
                        FontAttributes="Bold"
                        CornerRadius="5"
                        HeightRequest="40"
                        Margin="5" />

                <Button Text="QUITAR TODOS"
                        BackgroundColor="#71639e"
                        TextColor="White"
                        FontAttributes="Bold"
                        CornerRadius="5"
                        HeightRequest="40"
                        Grid.Column="1"
                        Margin="5" />
            </Grid>

            <Grid Grid.Row="3" 
                  ColumnDefinitions="50,*,Auto" 
                  Padding="5">
                <Label Text="N.L." FontAttributes="Bold" TextColor="Black"/>
                <Label Text="Nombre" FontAttributes="Bold" TextColor="Black" Grid.Column="1"/>
                <Label Text="Activo" FontAttributes="Bold" TextColor="Black" Grid.Column="2"/>
            </Grid>

            <Border Grid.Row="4"
                    Stroke="LightGray"
                    StrokeShape="RoundRectangle 10"
                    BackgroundColor="White"
                    Margin="5">
                <CollectionView x:Name="ListaUsuarios"
                              ItemsSource="{Binding Lista}"
                              SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="10,5" ColumnDefinitions="50,*,Auto">
                                <Label Text="{Binding Numero}" TextColor="Black"/>
                                <Label Text="{Binding Nombre}" Grid.Column="1" TextColor="Black"/>
                                <Switch IsToggled="{Binding Activo}" Grid.Column="2" HorizontalOptions="End"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Border>
        </Grid>
    </ScrollView>
</ContentPage>

